export default function LoginPage() {
  return (
    <main style={{ padding: 40 }}>
      <h1>Staff Login</h1>
      <p style={{ color: "var(--muted)" }}>
        Auth — Phase 2 build in progress.
      </p>
    </main>
  );
}
